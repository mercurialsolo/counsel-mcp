# Patterns to ignore in secret detection
# Each line is a regex pattern matched against file paths

# Test files may contain example secrets
tests/.*\.test\.ts

# Documentation may contain example patterns
.*\.md$

# Lock files
package-lock\.json
